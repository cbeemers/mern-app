
const account = {
    minHeight: "-webkit-calc(100%)",
    backgroundColor: "#192635"
}

const border = {
    border: "2px solid white",
    padding: "2em",
    marginBottom: "8em",
}

const calendar = {
    borderRadius: "20%",
    height: "3em",
    minWidth: "4em",
    margin: "1em",
    opacity: "1",
    color: "#170a49",
    border: "none",
    // backgroundColor: "#520012",
    backgroundColor: "#007bff",
    // marginBottom: "3em",
}

const form_style = {
    textAlign: "center",
    margin: "auto",
    marginTop: "5em",
    color: "grey",
    width: "25em",
}

const label = {
    float: "left"
}

const msg = {
    textAlign: "center",
    paddingTop: "2em",
    backgroundColor: "#192635",
    color: "grey",
}

const search = {
    textAlign: "center",
    padding: "2em 0",
    backgroundColor: "#192635",
}

const stockWidget = {
    margin: "auto",
    textAlign: "center",
}

const widget = {
    color: "grey",
}



export {form_style, label, border, search, widget, account, msg, stockWidget, calendar};